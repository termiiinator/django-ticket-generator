# Генератор Экзаменационных Билетов

Django-приложение для генерации экзаменационных билетов по различным предметам с поддержкой специальной логики для физики (5 уровней сложности).

## Возможности

- 🎓 Генерация билетов для 5 предметов (Анализ Алгоритмов, Комплексный Анализ, Дифференциальные уравнения, Численные методы, Физика)
- 📝 Стандартные предметы: 2 теоретических + 3 практических вопроса
- ⚛️ Физика: 5 вопросов разных уровней сложности
- 📊 Управление базой вопросов
- 📥📤 Импорт/экспорт вопросов в JSON
- 🖨️ Печать билетов в формате A4 landscape
- 📈 Статистика по вопросам
- 🔍 Фильтрация и поиск вопросов

## Установка

### 1. Клонировать или скачать проект

```bash
cd exam_tickets_django
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
```

### 3. Активировать виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 4. Установить зависимости

```bash
pip install -r requirements.txt
```

### 5. Применить миграции базы данных

```bash
python manage.py migrate
```

### 6. Создать суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

### 7. Запустить сервер разработки

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: **http://127.0.0.1:8000/**

## Использование

### Добавление вопросов

1. Перейдите на страницу "Управление вопросами"
2. Используйте форму "Массовое добавление вопросов"
3. Выберите предмет, тип/уровень
4. Введите вопросы (каждый с новой строки)
5. Нажмите "Добавить"

### Генерация билета

1. На главной странице выберите предмет
2. Нажмите "Сгенерировать билет"
3. Используйте кнопку "Печать билета" или "Копировать текст"

### Импорт начальных данных

Проект поддерживает импорт из JSON. Пример формата:

```json
[
  {
    "subject": "Математика",
    "type": "theory",
    "text": "Сформулируйте теорему Пифагора"
  },
  {
    "subject": "Физика",
    "type": "theory",
    "text": "Что такое сила?",
    "difficulty": 1
  }
]
```

## Структура проекта

```
exam_tickets_django/
├── exam_tickets/          # Настройки проекта
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── tickets/               # Приложение
│   ├── models.py         # Модели БД
│   ├── views.py          # Представления
│   ├── urls.py           # URL-маршруты
│   └── admin.py          # Админка
├── templates/
│   └── tickets/          # HTML шаблоны
│       ├── base.html
│       ├── index.html
│       ├── ticket.html
│       └── questions.html
├── manage.py
└── requirements.txt
```

## Модели данных

### Question (Вопрос)
- `subject` - предмет
- `type` - тип (теория/практика)
- `text` - текст вопроса
- `difficulty` - уровень сложности (для физики)

### TicketCounter (Счетчик билетов)
- `subject` - предмет
- `last_number` - последний номер билета

## Требования для генерации

**Стандартные предметы:**
- Минимум 2 теоретических вопроса
- Минимум 3 практических задания

**Физика:**
- Минимум по 1 вопросу каждого уровня (1-5)

## Админ-панель

Доступна по адресу: **http://127.0.0.1:8000/admin/**

После создания суперпользователя вы можете управлять вопросами через стандартную админ-панель Django.

## Печать билетов

Билеты оптимизированы для печати на формате A4 landscape с параметрами:
- Размер: 250mm x 135mm
- Шрифт: Times New Roman
- Поддержка печати через браузер (Ctrl+P)

## Технологии

- Python 3.8+
- Django 4.2+
- SQLite (база данных)
- HTML/CSS (чистый, без фреймворков)

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Перенесено с Next.js + TypeScript на Django + Python
